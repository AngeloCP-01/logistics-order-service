# logistics-order-service

Order CRUD, lifecycle state machine, history, and scheduling for the AI Logistics Platform. Neon Postgres. Publishes `order.*` events and consumes `dispatch.driver.assigned` + `delivery.completed`.

> **Status:** scaffolded — implementation pending Phase 3 (see `CLAUDE.md`).
